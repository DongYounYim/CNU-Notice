const Icon = ({ children }) => {
  return (
    <div
      style={{
        display: "inline-block",
        verticalAlign: "middle",
        textAlign: "center",
      }}
    >
      {children}
    </div>
  );
};

export default Icon;
